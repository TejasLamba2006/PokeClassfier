# Database
DATABASE_URL="postgresql://username:password@localhost:5432/pokeclassifier"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Admin Configuration
ADMIN_EMAIL="your-admin-email@example.com"

# Discord OAuth (replace Google OAuth)
DISCORD_CLIENT_ID="your-discord-client-id"
DISCORD_CLIENT_SECRET="your-discord-client-secret"

# Discord Server URL for Premium Access
NEXT_PUBLIC_DISCORD_URL="https://discord.gg/your-server-invite"

# Upstash Redis (for rate limiting)
UPSTASH_REDIS_REST_URL="https://your-redis-url.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-redis-token"

# Google Gemini API (for AI analysis - Free alternative to OpenAI)
GOOGLE_GENERATIVE_AI_API_KEY="your-google-gemini-api-key"

# OpenAI API (alternative - paid)
# OPENAI_API_KEY="sk-your-openai-api-key"

# OCR Configuration
# Choose your OCR provider by uncommenting the relevant option:

# Option 1: Tesseract.js (Default - Free, works offline, no API key needed)
# No configuration required - works out of the box!

# Option 2: OCR.space API (Freemium - 25k requests/month free)
# OCR_SPACE_API_KEY="your-ocr-space-api-key"

# Option 3: Google Cloud Vision API (Paid - highest accuracy)
# Get these from Google Cloud Console:
# 1. Create project at https://console.cloud.google.com/
# 2. Enable Vision API
# 3. Create service account with Vision API access
# 4. Download JSON key file
# 5. Set these environment variables:
GOOGLE_CLOUD_PROJECT_ID="your-project-id"
GOOGLE_CLOUD_KEY_FILE="/absolute/path/to/service-account-key.json"

# Node Environment
NODE_ENV="development"
